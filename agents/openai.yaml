interface:
  display_name: "Firmware Doc Reviewer"
  short_description: "按文档类型和评审视角路由 PRD/PFS 规则并输出可审计的结构化结论"
  default_prompt: "Use $firmware-doc-reviewer to review this PRD, product requirements document, or firmware PFS with the matching rules."
