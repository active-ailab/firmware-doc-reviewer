interface:
  display_name: "Firmware Doc Reviewer"
  short_description: "按评审视角路由 PRD 规则并输出可审计的结构化结论"
  default_prompt: "Use $firmware-doc-reviewer to review this PRD or product requirements document with the matching rules."
