---
name: firmware-doc-reviewer
description: 固件侧统一文档评审 Skill。当前仅支持固件侧 PRD Review；根据用户明确给出的固件视角加载 rules/ 下对应规则，并输出可审计的固件评审结论。
---

# Firmware Doc Reviewer

作为固件侧文档评审的统一入口。当前仅支持固件侧 PRD Review；后续可在 `rules/` 下增加其他视角或文档类型的独立规则文件。

## 路由

按以下顺序识别评审视角和文档类型：

1. 请求明确包含“固件侧”“固件”“手表端”“嵌入式”“firmware”或等价固件评审视角，且命中 `PRD`、`产品需求文档`、`需求评审` 或等价表达时，完整读取并执行 [`rules/prd-review-fw.md`](rules/prd-review-fw.md)。
2. 请求仅包含 `PRD review`、`PRD`、`产品需求文档`、`需求评审` 或等价表达，但未明确评审视角时，必须请用户选择“固件侧”或“通用（产品经理视角，后续接入）”；不得套用固件侧规则。
3. 用户选择“通用（产品经理视角）”时，说明该视角尚未接入；不得套用固件侧规则。
4. 无法匹配已有规则时，说明当前不支持该文档类型或视角，并列出已识别的关键词；不要套用固件侧 PRD Review 规则。

只加载当前评审视角和文档类型需要的规则文件，不预加载其他规则。

## 通用边界

- 默认只读：不修改被评审文档，不创建 Jira，不提交代码，不发布固件。
- 以当前文档及其明确关联资料为证据，不把推断写成事实。
- 关联资料无法访问时，明确记录缺口及其影响。
- 先按 PRD 原文提取目标产品、首发设备、适配范围和验收设备；不根据型号名称、产品知识或章节语义推断设备关系。PRD 未列目标机型或项目清单时，默认评估所有支持该功能的项目机器；未明确写出支持条件时，默认设备具备支持条件，不因缺少清单或条件扣分或设 Block。
- 固件版本和 App 版本不是默认评审条件；只有 PRD 明确涉及版本差异、升级、降级、兼容或灰度时，才纳入版本评审。
- 固件侧 PRD Review 结论必须使用 `rules/prd-review-fw.md` 定义的固定输出模板；保留适用一级、二级标题和顺序，与固件无关的维度和专属明细直接跳过，不输出“不适用”，不得自由改动报告结构。
- 代码仓库不是默认评审输入；只有具体规则允许且触发其可选条件时才检查。

## 扩展新视角或文档类型

新增评审视角或文档类型时：

1. 在 `rules/` 下增加一个对应的 Markdown 规则文件。
2. 在本文件“路由”中补充识别关键词和目标规则文件。
3. 不创建新的 Skill。

---

Owner: cs-dongqi@zepp.com  
Organization: Active.Bu
