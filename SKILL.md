---
name: firmware-doc-reviewer
description: 统一 PRD 与固件侧 PFS 评审 Skill。按文档类型和评审视角加载对应规则，并输出可审计的结构化结论。
---

# Firmware Doc Reviewer

作为 PRD 文档评审与固件侧 PFS 评审的统一入口。当前支持固件侧 PFS、固件侧 PRD 与通用（产品经理）PRD；后续可在 `rules/` 下增加其他视角或文档类型的独立规则文件。

## 路由

按以下顺序识别评审视角和文档类型：

1. 请求明确包含 `PFS`、`PFS Review`、“PFS 评审”、“产品功能规格书”或“功能规格说明”等固定 PFS 结构表达时，完整读取并执行 [`rules/pfs-review-fw.md`](rules/pfs-review-fw.md)。PFS 路由优先于所有 PRD 路由；PFS 是评审对象，明确引用的 PRD 是基准证据。
2. 请求明确包含“固件侧”“固件”“手表端”“嵌入式”“firmware”或等价固件评审视角，且命中 `PRD`、`产品需求文档`、`需求评审` 或等价表达时，完整读取并执行 [`rules/prd-review-fw.md`](rules/prd-review-fw.md)。即使同时出现通用或产品经理视角，仍优先固件侧规则。
3. 请求明确包含“通用 PRD”“产品经理视角”“产品侧”“PM 视角”，且命中 PRD 表达时，完整读取并执行 [`rules/prd-review.md`](rules/prd-review.md)。
4. 仅包含 `PRD review`、`PRD`、`产品需求文档`、`需求评审` 或等价表达时，默认完整读取并执行 [`rules/prd-review.md`](rules/prd-review.md)。
5. 后续新增明确视角时，在本路由中增加其规则，并置于默认通用路由之前。
6. 无法匹配已有规则时，说明当前不支持该文档类型或视角，并列出已识别的关键词；不得强行套用规则。

只加载当前评审视角和文档类型需要的规则文件，不预加载其他规则。

## 通用边界

- 默认只读：不修改被评审文档，不创建 Jira，不提交代码，不发布固件。
- 以当前文档及其明确关联资料为证据，不把推断写成事实。
- 关联资料无法访问时，明确记录缺口及其影响。
- 每种 PRD 视角均以当前 PRD 和明确关联资料为证据；PFS 规则以 PFS 和可访问、唯一的基准 PRD 为证据。无法访问的关联资料仅在实质影响对应视角的决策、拆分、交付或验收时设为 Block。
- 固件设备、版本、端侧归属和代码核查边界只适用于固件侧规则；通用规则的研发评估不要求代码、协议或架构方案。
- 每种 Review 结论必须使用其规则文件定义的固定输出模板；只输出适用维度和事项，不输出“不适用”。

## 扩展新视角或文档类型

新增评审视角或文档类型时：

1. 在 `rules/` 下增加一个对应的 Markdown 规则文件。
2. 在本文件“路由”中补充识别关键词和目标规则文件。
3. 不创建新的 Skill。

---

Owner: cs-dongqi@zepp.com  
Organization: Active.Bu
